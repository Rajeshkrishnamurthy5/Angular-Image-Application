import { ImageAppDirective } from './image-app.directive';

describe('ImageAppDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageAppDirective();
    expect(directive).toBeTruthy();
  });
});
